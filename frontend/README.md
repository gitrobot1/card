# Card Hub Frontend

Vue 3 + TypeScript + Vite 前端，斗地主对局 UI 与动画。

完整说明见项目根目录 [README.md](../README.md)。

## 本地开发

```bash
./scripts/dev.sh
```

默认端口：`6677`

## 构建

```bash
npm install
npm run build
```

产物输出到 `dist/`（已加入 `.gitignore`）。
